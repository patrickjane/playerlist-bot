# This workflow will build a golang project
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-go

name: Release bot

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

permissions:
  contents: write
  packages: write

jobs:
  build-release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v4
        with:
          go-version: "1.25.7"

      - name: Build
        run: |
          export BUILD_VERSION=${{ github.ref_name }}
          ./create_binaries.sh

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Packages
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build Docker image
        run: |
          export BUILD_VERSION=${{ github.ref_name }}
          export BUILD_VERSION=v1.0.0

          export IMAGE=ghcr.io/${{ github.repository }}

          cp playerlistbot.linux-amd64 build/playerlistbot.linux-amd64
          cp playerlistbot.linux-arm64 build/playerlistbot.linux-arm64
          cd build

          docker buildx build \
              --platform linux/amd64,linux/arm64 \
              --build-arg VERSION=${VERBUILD_VERSIONSION} \
              -t ${IMAGE}:${BUILD_VERSION} \
              -t ${IMAGE}:latest \
              --push \
              .

      - name: Create Release
        uses: softprops/action-gh-release@v1
        if: startsWith(github.ref, 'refs/tags/')
        with:
          tag_name: ${{ github.ref_name }}
          files: |
            playerlistbot.darwin-amd64
            playerlistbot.darwin-arm64
            playerlistbot.linux-amd64
            playerlistbot.linux-arm64
            playerlistbot.windows-amd64.exe
